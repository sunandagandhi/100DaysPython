#Problem Statement: Write a program to identify if the number is Prime number or not

num=int(input("Enter number: "))
x=False

if num==1:
    print("It is not a prime number")
if num>1:
    for i in range(2,num):
        if num%i==0:
            x=True
        break

    if x:
        print("It is not a prime number")
    else:
        print("It is prime number")
