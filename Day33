#Problem Statement: Python program to check if a string is palindrome or not

my_string=input("Enter string:") 
if(my_string==my_string[::-1]): 
 print("The string is a palindrome") 
else: 
 print("The string isn't a palindrome") 
